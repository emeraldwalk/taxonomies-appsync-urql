{
  "name": "taxonomies",
  "private": "true",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn workspace client start",
    "build:server": "yarn workspace server build",
    "deploy": "yarn workspace server cdk deploy",
    "synth": "yarn workspace server cdk synth"
  },
  "dependencies": {},
  "devDependencies": {},
  "workspaces": [
    "packages/*"
  ]
}
