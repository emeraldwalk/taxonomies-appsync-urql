{
  "name": "taxonomies",
  "private": "true",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn workspace client start",
    "build:server": "yarn workspace server build",
    "deploy": "yarn build:server && yarn workspace server cdk deploy",
    "synth": "yarn workspace server cdk synth",
    "codegen": "yarn workspace client codegen"
  },
  "dependencies": {},
  "devDependencies": {},
  "workspaces": [
    "packages/*"
  ]
}
