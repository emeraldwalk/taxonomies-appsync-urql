type Category {
  name: String!
  value: String!
}

type Content {
  categories: [Category!]
  tags: [String!]
  value: String!
}

type Query {
  getContent: [Content!]!
  putContent(id: String, value: String!): Boolean!
}
